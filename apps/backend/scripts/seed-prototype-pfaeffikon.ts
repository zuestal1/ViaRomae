#!/usr/bin/env tsx
/**
 * Compatibility entry point for the Pfäffikon prototype seed.
 *
 * Keep the implementation in one place so this historic command cannot drift
 * away from the current database schema again.
 */
import './seed-prototype-pfaeffikon-fixed.js';
